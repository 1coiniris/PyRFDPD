# Add your neural network configuration here
title = "volterra_nn"
owner = "CRZ"

[model]
model_name = "volterra_nn"
memory_depth = 7
hidden_layers = [64, 64, 64, 64]
activation = "ReLU"

[model.hyperparameters]
train_ratio = 0.8
iteration = 3
learning_rate = 0.001
batch_size = 128
loss_function = "nn.MSELoss()"
optimizer = "optim.Adam"
epochs = 50